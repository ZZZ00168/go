package main

import (
		"fmt"
		"time"
)
func main() {
        start := time.Now()
        parseUrls("http://a.0516meiya.com/play/3892.html")
        elapsed := time.Since(start)
        fmt.Printf("Took %s", elapsed)
}